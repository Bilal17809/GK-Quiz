import 'package:flutter/material.dart';

const fontFamily = 'Montserrat';
const double kBodyHp = 16;
const kAppBarPadding = EdgeInsets.only(left: 8.0, top: 10, bottom: 10);
const kGridPadding = EdgeInsets.all(8);
